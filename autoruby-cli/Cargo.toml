[package]
edition = "2021"
name = "autoruby-cli"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
autoruby = {path = "../autoruby"}
clap = {version = "4.0.30", features = ["derive"]}
dotenvy = "0.15.6"
